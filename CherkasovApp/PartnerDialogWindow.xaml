<Window x:Class="CherkasovApp.PartnerDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование партнера" Height="500" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Наименование -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Наименование:" 
                   VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="NameTextBox" 
                 Margin="5" Padding="3"/>

        <!-- Тип партнера -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Тип партнера:" 
                   VerticalAlignment="Center" Margin="5"/>
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="TypeComboBox" 
                  DisplayMemberPath="Name" SelectedValuePath="Id"
                  Margin="5" Padding="3"/>

        <!-- Рейтинг -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Рейтинг:" 
                   VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="RatingTextBox" 
                 Margin="5" Padding="3" PreviewTextInput="NumberValidationTextBox"/>

        <!-- Скидка (только для чтения) -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Скидка:" 
                   VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="DiscountTextBox" 
                 Margin="5" Padding="3" IsReadOnly="True" 
                 Background="#F0F0F0" Foreground="Green" FontWeight="Bold"/>

        <!-- АДРЕС (новая строка) -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Адрес:" 
                   VerticalAlignment="Top" Margin="5"/>
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="AddressTextBox" 
                 Margin="5" Padding="3" TextWrapping="Wrap" 
                 AcceptsReturn="True" MinHeight="60" VerticalScrollBarVisibility="Auto"/>

        <!-- ФИО директора -->
        <TextBlock Grid.Row="5" Grid.Column="0" Text="ФИО директора:" 
                   VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="5" Grid.Column="1" x:Name="DirectorTextBox" 
                 Margin="5" Padding="3"/>

        <!-- Телефон -->
        <TextBlock Grid.Row="6" Grid.Column="0" Text="Телефон:" 
                   VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="6" Grid.Column="1" x:Name="PhoneTextBox" 
                 Margin="5" Padding="3"/>

        <!-- Email -->
        <TextBlock Grid.Row="7" Grid.Column="0" Text="Email:" 
                   VerticalAlignment="Center" Margin="5"/>
        <TextBox Grid.Row="7" Grid.Column="1" x:Name="EmailTextBox" 
                 Margin="5" Padding="3"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="9" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="5">
            <Button x:Name="SaveButton" Content="Сохранить" Width="80" 
                    Margin="5" Click="SaveButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="80" 
                    Margin="5" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>